<!DOCTYPE html>
<html lang="en">
<head>
    <title>Home</title>

<link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet">
<style>
    .error-notice {
      margin: 5px 5px; /* Making sure to keep some distance from all side */
    }

    .oaerror {
      width: 90%; /* Configure it fit in your design  */
      margin: 0 auto; /* Centering Stuff */
      background-color: #FFFFFF; /* Default background */
      padding: 20px;
      border: 1px solid #eee;
      border-left-width: 5px;
      border-radius: 3px;
      margin: 0 auto;
      font-family: 'Open Sans', sans-serif;
      font-size: 16px;
    }

    .danger {
      border-left-color: #d9534f; /* Left side border color */
      background-color: rgba(217, 83, 79, 0.1); /* Same color as the left border with reduced alpha to 0.1 */
    }

    .danger strong {
      color:  #d9534f;
    }

    .warning {
      border-left-color: #f0ad4e;
      background-color: rgba(240, 173, 78, 0.1);
    }

    .warning strong {
      color: #f0ad4e;
    }

    .info {
      border-left-color: #5bc0de;
      background-color: rgba(91, 192, 222, 0.1);
    }

    .info strong {
      color: #5bc0de;
    }

    .success {
      border-left-color: #2b542c;
      background-color: rgba(43, 84, 44, 0.1);
    }

    .success strong {
      color: #2b542c;
    }
</style>

<body>
<ul class="nav nav-tabs">
  <li role="presentation" class="active"><a href="{% url 'home' %}">Home</a></li>
  <li role="presentation"><a href="{% url 'restfull_api' %}">Restful API</a></li>
</ul>


<br><br>
<div class="container">
    <div>
    <form action="{% url 'home' %}" method="POST">{% csrf_token %}
        <div class="form-group col-md-12 ">                                
        <label id="messageLabel" for="message">Message </label>
        <textarea class="form-control input-sm"  type="text" name="userinput" rows="7"></textarea>
        </div>
        <br style="clear:both">
        <div class="form-group col-md-2">
        <br>
        <input class="input-sm btn" type="submit" value="Submit" style="height:35px">
    </form>
    </div>
</div>

<br><br><br><br>
<div class="container">
	<div class="row">
     <br><br>
          <div class="oaerror success">
            <strong>Original</strong> - {{data.input}}
          </div><br>

          <div class="oaerror success">
            <strong>Numeric</strong> - {{data.numeric}}
          </div><br>

          <div class="oaerror success">
            <strong>Sorted</strong> - {{data.sorted}}
          </div><br>
        </div>
	</div>
</div>

 
</body>
</html>
